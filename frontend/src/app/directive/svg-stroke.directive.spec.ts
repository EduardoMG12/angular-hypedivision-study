import { SvgStrokeDirective } from './svg-stroke.directive';

describe('SvgStrokeDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgStrokeDirective();
    expect(directive).toBeTruthy();
  });
});
